# 📝 Python To-Do List Manager (CLI + JSON Storage)

A simple and beginner-friendly **Python To-Do List Application** built using a menu-driven command-line interface.  
This project supports **adding, viewing, deleting, and marking tasks as done**, with **auto-save** functionality using a `tasks.json` file.

---

## 🚀 Features

✔ Add new tasks  
✔ View all tasks with status (Done / Pending)  
✔ Delete tasks  
✔ Mark tasks as completed  
✔ Tasks automatically saved in `tasks.json`  
✔ Tasks load automatically every time the program starts  
✔ Clean and simple CLI menu  
✔ Beginner-friendly Python project  

---

## 📂 Project Structure

